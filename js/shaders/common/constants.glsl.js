export const constantsGLSL = `

const float FBM_SCALE = 3.0;

const float GLOW_SCALE = 8.0;

const float MIN_ALPHA = 0.10;
const float MAX_ALPHA = 0.45;

const float MIN_BRIGHTNESS = 0.92;
const float MAX_BRIGHTNESS = 1.00;

`;
